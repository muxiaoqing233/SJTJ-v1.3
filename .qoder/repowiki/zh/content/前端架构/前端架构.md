# 前端架构

<cite>
**本文档中引用的文件**  
- [index.html](file://index.html)
- [knowledge-graph.html](file://knowledge-graph.html)
- [qa.html](file://qa.html)
- [recommendation.html](file://recommendation.html)
- [templates/header.html](file://templates/header.html)
- [templates/footer.html](file://templates/footer.html)
- [styles/common.css](file://styles/common.css)
- [styles/knowledge-graph.css](file://styles/knowledge-graph.css)
- [styles/qa.css](file://styles/qa.css)
- [styles/recommendation.css](file://styles/recommendation.css)
- [scripts/common.js](file://scripts/common.js)
- [scripts/knowledge-graph.js](file://scripts/knowledge-graph.js)
- [scripts/qa.js](file://scripts/qa.js)
- [scripts/weapon-image-integration.js](file://scripts/weapon-image-integration.js)
- [styles/weapon-image-lightbox.css](file://styles/weapon-image-lightbox.css)
</cite>

## 目录
1. [项目结构](#项目结构)
2. [HTML模板结构](#html模板结构)
3. [CSS样式组织](#css样式组织)
4. [JavaScript脚本分工](#javascript脚本分工)
5. [前端与后端交互](#前端与后端交互)
6. [D3.js知识图谱渲染](#d3js知识图谱渲染)
7. [Lightbox图片展示](#lightbox图片展示)
8. [页面功能与用户交互](#页面功能与用户交互)

## 项目结构

本项目采用模块化前端架构，主要包含以下目录结构：
- `templates/`：存放公共HTML模板文件，如header.html和footer.html
- `styles/`：存放CSS样式文件，按功能模块划分
- `scripts/`：存放JavaScript脚本文件，按功能模块划分
- `images/`：存放项目图片资源
- 根目录：存放主要HTML页面文件

这种结构实现了关注点分离，便于维护和扩展。

## HTML模板结构

### 页头模板 (header.html)

页头模板定义了网站的全局导航结构，包含logo和用户状态区域：

```html
<header>
    <div class="logo">兵智视界</div>
    <div class="user-status">
        <a href="login.html" class="btn">登录</a>
    </div>
</header>
```

**模板来源**
- [templates/header.html](file://templates/header.html)

### 页脚模板 (footer.html)

页脚模板包含版权信息：

```html
<footer>
    <p>&copy; 2025 兵智视界 - 南京理工大学紫金学院</p>
</footer>
```

**模板来源**
- [templates/footer.html](file://templates/footer.html)

### 模板集成

各页面通过JavaScript动态加载这些模板，实现一致的页面布局。例如在qa.html中使用fetch API加载模板：

```javascript
fetch('templates/header.html')
    .then(response => response.text())
    .then(data => {
        document.getElementById('header-placeholder').innerHTML = data;
    });
```

**模板集成来源**
- [qa.html](file://qa.html#L70-L78)

## CSS样式组织

### 公共样式 (common.css)

`common.css`文件定义了全局样式变量和基础组件样式：

```css
:root {
    --primary-color: #2c3e50;
    --secondary-color: #3498db;
    --accent-color: #1abc9c;
    --dark-bg: #0a0e17;
    --card-bg: #131a27;
    --text-color: #e0e0e0;
}
```

该文件包含导航栏、按钮、卡片等通用UI组件的样式定义，确保整个应用的视觉一致性。

**公共样式来源**
- [styles/common.css](file://styles/common.css#L1-L421)

### 页面特定样式

每个主要页面都有对应的CSS文件，实现特定的视觉设计：

#### 知识图谱样式

`knowledge-graph.css`定义了知识图谱页面的布局和交互样式，采用网格布局将页面分为三个区域：图例、可视化区域和节点详情。

```css
.knowledge-graph-container {
    display: grid;
    grid-template-columns: 200px 1fr 250px;
    gap: 20px;
}
```

**知识图谱样式来源**
- [styles/knowledge-graph.css](file://styles/knowledge-graph.css#L1-L799)

#### 智能问答样式

`qa.css`文件使用Flexbox布局创建聊天界面，包含消息窗口、输入区域和工具栏。

```css
.qa-container {
    display: flex;
    flex-direction: column;
    height: 75vh;
}
```

**智能问答样式来源**
- [styles/qa.css](file://styles/qa.css#L1-L425)

#### 智能推荐样式

`recommendation.css`定义了推荐卡片的样式，包含悬停效果和标签样式。

```css
.recommendation-card {
    border-left: 4px solid var(--accent-color);
    transition: transform 0.3s, box-shadow 0.3s;
}
```

**智能推荐样式来源**
- [styles/recommendation.css](file://styles/recommendation.css#L1-L164)

### 响应式设计

所有CSS文件都包含媒体查询，确保在不同设备上的良好显示效果：

```css
@media (max-width: 768px) {
    nav ul {
        flex-direction: column;
        align-items: center;
    }
}
```

## JavaScript脚本分工

### 公共脚本 (common.js)

`common.js`负责导航菜单的激活状态管理，根据当前页面URL自动为相应的导航项添加active类：

```javascript
document.addEventListener('DOMContentLoaded', function() {
    const currentPage = window.location.pathname.split('/').pop() || 'index.html';
    document.querySelectorAll('nav ul li a').forEach(link => {
        const href = link.getAttribute('href');
        if (href === currentPage) {
            link.classList.add('active');
        }
    });
});
```

**公共脚本来源**
- [scripts/common.js](file://scripts/common.js#L1-L17)

### 知识图谱脚本 (knowledge-graph.js)

`knowledge-graph.js`是知识图谱的核心脚本，负责：

1. 初始化D3.js力导向图
2. 连接Neo4j数据库获取图数据
3. 渲染节点和连接线
4. 处理节点点击事件显示详情
5. 集成图片、视频和3D模型管理功能

```javascript
function renderGraph(data) {
    const svg = d3.select('#graph-visualization')
        .append('svg')
        .attr('width', '100%')
        .attr('height', '100%');
    
    // 创建节点和连接线
    const node = svg.append('g')
        .selectAll('.node-group')
        .data(data.nodes)
        .enter().append('g')
        .attr('class', 'node-group');
}
```

**知识图谱脚本来源**
- [scripts/knowledge-graph.js](file://scripts/knowledge-graph.js#L1-L799)

### 智能问答脚本 (qa.js)

`qa.js`实现与DeepSeek API的集成，支持流式响应和Markdown渲染：

```javascript
async function streamChatCompletion(prompt, outputElement, onContentUpdate) {
    const response = await fetch(DEEPSEEK_API_URL, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'Authorization': `Bearer ${DEEPSEEK_API_KEY}`
        },
        body: JSON.stringify(requestBody),
        signal: currentAbortController?.signal
    });
}
```

该脚本还实现了API密钥管理、错误处理和用户界面更新。

**智能问答脚本来源**
- [scripts/qa.js](file://scripts/qa.js#L1-L433)

### 武器图片集成脚本 (weapon-image-integration.js)

`weapon-image-integration.js`实现完整的图片管理功能，包括上传、查看、编辑和删除：

```javascript
class WeaponImageManager {
    constructor() {
        this.currentWeaponId = null;
        this.currentWeaponName = null;
        this.isUploading = false;
        this.init();
    }
    
    openLightbox(imageId) {
        const images = this.getCurrentImages();
        const image = images.find(img => img.id === imageId);
        
        const lightboxHtml = `
            <div class="weapon-image-lightbox" id="weaponImageLightbox">
                <div class="lightbox-content">
                    <img class="lightbox-image" src="http://localhost:3001${image.path}">
                    <div class="lightbox-info">
                        <div class="lightbox-title">${image.originalName}</div>
                    </div>
                </div>
            </div>
        `;
    }
}
```

**武器图片集成脚本来源**
- [scripts/weapon-image-integration.js](file://scripts/weapon-image-integration.js#L1-L640)

## 前端与后端交互

### REST API通信

前端通过fetch API与后端REST API进行通信，主要交互模式包括：

#### GET请求 - 数据获取

```javascript
const response = await fetch('http://localhost:3001/api/knowledge/graph-data');
const data = await response.json();
```

#### POST请求 - 数据创建

```javascript
const response = await fetch(`http://localhost:3001/api/weapon-images/${weaponId}`, {
    method: 'POST',
    headers: {
        'x-admin-user': 'true'
    },
    body: formData
});
```

**API交互来源**
- [function_description/前后端连接实现详解.md](file://function_description/前后端连接实现详解.md#L28-L77)
- [scripts/knowledge-graph.js](file://scripts/knowledge-graph.js#L700-L730)
- [scripts/weapon-image-integration.js](file://scripts/weapon-image-integration.js#L300-L330)

### 后端服务端点

前端主要与以下后端API端点交互：

- `/api/knowledge/graph-data`：获取知识图谱数据
- `/api/weapon-images/{id}`：获取、上传、更新和删除武器图片
- `/api/manufacturers`：获取制造商列表
- `/health`：检查后端服务状态

## D3.js知识图谱渲染

### 力导向图实现

知识图谱使用D3.js的力导向图布局实现动态可视化：

```javascript
const simulation = d3.forceSimulation()
    .force('link', d3.forceLink().id(d => d.id).distance(100))
    .force('charge', d3.forceManyBody().strength(-200))
    .force('center', d3.forceCenter(width / 2, height / 2))
    .force('collision', d3.forceCollide().radius(30));
```

### 节点和连接线渲染

脚本创建了节点组、连接线和标签，并应用了颜色编码：

```javascript
const node = svg.append('g')
    .selectAll('.node-group')
    .data(data.nodes)
    .enter().append('g')
    .attr('class', 'node-group');

node.append('circle')
    .attr('class', 'node')
    .attr('r', 15)
    .attr('fill', d => nodeColors[d.labels[0]] || nodeColors.default);
```

### 交互功能

实现了丰富的交互功能：
- 节点点击显示详细信息
- 拖拽节点重新布局
- 缩放和平移视图
- 动态过滤节点和连接线

```javascript
node.on('click', function(event, d) {
    selectedNode = this;
    d3.select(this).select('circle').classed('selected', true);
    displayNodeDetails(d);
});
```

**D3.js实现来源**
- [scripts/knowledge-graph.js](file://scripts/knowledge-graph.js#L100-L300)

## Lightbox图片展示

### 灯箱样式

`weapon-image-lightbox.css`文件定义了灯箱的视觉效果，包括动画、阴影和响应式设计：

```css
.weapon-image-lightbox {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.9);
    z-index: 10000;
    display: none;
    justify-content: center;
    align-items: center;
    backdrop-filter: blur(5px);
    animation: lightboxFadeIn 0.3s ease-out;
}
```

### 灯箱功能

`WeaponImageManager`类实现了完整的灯箱功能：

```javascript
openLightbox(imageId) {
    const images = this.getCurrentImages();
    const image = images.find(img => img.id === imageId);
    
    const lightboxHtml = `
        <div class="weapon-image-lightbox" id="weaponImageLightbox">
            <div class="lightbox-content">
                <button class="lightbox-close" onclick="weaponImageManager.closeLightbox()">
                    <i class="fas fa-times"></i>
                </button>
                <img class="lightbox-image" src="http://localhost:3001${image.path}">
                <div class="lightbox-info">
                    <div class="lightbox-title">${image.originalName}</div>
                    <div class="lightbox-description">${image.description}</div>
                </div>
            </div>
        </div>
    `;
}
```

### 用户交互

灯箱支持以下交互：
- 点击关闭按钮或背景关闭灯箱
- 使用ESC键关闭灯箱
- 查看图片详细信息（上传时间、文件大小等）
- 编辑和删除图片

**Lightbox实现来源**
- [styles/weapon-image-lightbox.css](file://styles/weapon-image-lightbox.css#L1-L342)
- [scripts/weapon-image-integration.js](file://scripts/weapon-image-integration.js#L500-L600)

## 页面功能与用户交互

### 首页 (index.html)

首页包含轮播图、快速链接和仪表盘，提供网站导航入口：

```html
<section class="carousel-section">
    <div class="swiper-container">
        <div class="swiper-wrapper">
            <div class="swiper-slide" onclick="showWeaponDetails('tank')">
                <img src="images/tank.png" alt="坦克">
            </div>
        </div>
    </div>
</section>
```

**首页来源**
- [index.html](file://index.html#L1-L234)

### 知识图谱页面 (knowledge-graph.html)

知识图谱页面提供完整的图谱可视化和数据分析功能：

```html
<div id="graph-container" class="knowledge-graph-container">
    <div class="graph-legend">...</div>
    <div id="graph-visualization" class="graph-visualization">...</div>
    <div class="node-details">...</div>
</div>
```

**知识图谱页面来源**
- [knowledge-graph.html](file://knowledge-graph.html#L1-L603)

### 智能问答页面 (qa.html)

智能问答页面实现聊天界面，支持Markdown渲染和代码高亮：

```html
<div class="qa-container">
    <div class="chat-window">...</div>
    <div class="input-area">
        <textarea placeholder="请输入您的问题..."></textarea>
        <button>发送</button>
    </div>
</div>
```

**智能问答页面来源**
- [qa.html](file://qa.html#L1-L78)

### 智能推荐页面 (recommendation.html)

智能推荐页面展示个性化内容推荐：

```html
<div class="recommendation-list">
    <div class="recommendation-card">
        <div class="recommendation-title">现代无人机技术的发展与应用</div>
        <div class="recommendation-tags">...</div>
        <div class="recommendation-description">...</div>
    </div>
</div>
```

**智能推荐页面来源**
- [recommendation.html](file://recommendation.html#L1-L119)